#include   <stdio.h>
int main() {
    //who is declaring the variable and what is the type of the variable
    //type variable_name = value;
    int age = 30; //integer variable
    float height = 1.75; //floating-point variable
    double weight = 70.5; //double variable
    // a diference between float and double is that double has a higher precision than float
    char grade = 'A'; //character variable
    char name[] = "John"; //string variable (array of characters)
    return 0;
    //rules of variable declaration
    //1. variable name must start with a letter or an underscore
    //2. variable name can only contain letters, digits, and underscores
    //3. variable name cannot be a reserved keyword
}